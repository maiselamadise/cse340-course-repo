<!-- src/views/organizations/detail.ejs -->

<h1>
  <%= organization.name %>
</h1>

<p>
  <%= organization.description %>
</p>

<h3>Projects</h3>

<% if(projects.length > 0) { %>

  <ul>

    <% projects.forEach(project => { %>

      <li>
        <a
          href="/projects/<%= project.project_id %>"
        >
          <%= project.project_name %>
        </a>
      </li>

    <% }) %>

  </ul>

<% } else { %>

  <p>No projects found.</p>

<% } %>